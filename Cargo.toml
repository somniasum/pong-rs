[package]
name = "pong"
version = "0.1.0"
edition = "2024"

[package.metadata.macroquad]
resource-dir = "assets"

[dependencies]
audio = "0.2.0"
quad-snd = "0.2.8"
macroquad = { version = "0.4", features = ["audio"], default-features = false }
